# 2026-09-18 — Item 10C (10A/10B ignorados por decisão)

## VENDAS paginada (relatórios)
- `?vendasPage=` 50/pg com contagem (`Página X de Y · Z vendas`), links preservando
  `start/end`; impressão exporta até 500 linhas com nota no subtitle.

## Rate-limit básico (`src/proxy.ts`)
- Bucket em memória (60 req/min/IP, janela 60s, evicção) **só para `/api/*`**;
  páginas/assets passam direto; 429 JSON. Sem nova dep.

## Auth negativa no repo
- `tests/e2e/auth-negative.spec.ts`: senha errada → mensagem, sem criar nada
  (contexto sem storageState).

## Dark mode
- `ThemeProvider` (next-themes, `attribute="class"`) no root + `ThemeToggle`
  (Sol/Lua) nos headers dashboard e admin. Variante `.dark` já existia no CSS.
- Prova: `dark-class: true`, toggle volta, 0 pageerrors, screenshot home-dark.

## Suítes
- Vitest 23/23 · Playwright 10/10 (`workers: 1` — dev local é o gargalo).
- `npm test` + `npm run test:e2e` (env `E2E_EMAIL/E2E_PASSWORD`, escrita só com `E2E_WRITE=1`).
