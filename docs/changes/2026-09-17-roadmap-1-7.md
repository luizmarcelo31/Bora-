# 2026-09-17 — Roadmap 1-7 (ordem do proprietário)

Ordem: 1→2→4→3→5→6→7 (itens 8-9 adiados).

## 1 — Cancelar venda (PDV)
- `cancelSaleAction` em `src/app/dashboard/pdv/actions.ts` (perm `sales.cancel`),
  botão em `/dashboard/pdv` nas vendas do dia.
- Ao cancelar: `SaleService.cancelSale` (reverte estoque + caixa) + estorno
  financeiro (DESPESA "Estorno PDV") + `logAudit`.

## 2 — Contas a pagar/receber com baixa
- `pay-actions.ts` em `/dashboard/financeiro` (toggle `paid/paidAt`),
  coluna Pago + botão Dar baixa/Desmarcar.
- Perm `financial.create` para baixar; audit `pay/unpay`.

## 4 — Divergências (fecha o caixa)
- `closeCashBoxAction` agora registra diferença sobra/falta como lançamento
  financeiro automático (RECEITA "Sobra de caixa" / DESPESA "Falta de caixa")
  vinculado ao caixa. Fecha o loop PDV→Caixa→Financeiro.

## 3 — Categorias
- Migration `20260917183200_add_categories`: `Category` (tenantId, name, kind
  PRODUCT/FINANCIAL) + enum `CategoryKind`.
- CRUD em `/dashboard/categorias` (create/toggle + audit), nav no tenant
  (Tag icon). Wire: select em `/dashboard/produtos` (PRODUCT) e
  `/dashboard/financeiro` (FINANCIAL). Validator `createCategorySchema`.

## 5 — Relatórios
- `/dashboard/relatorios` com filtro start/end, cards de vendas/faturado/saldo,
  top produtos (groupBy saleItem) e financeiro por categoria.
- Perm `reports.view`.

## 6 — Configurações do tenant
- `/dashboard/configuracoes` (upsert `TenantSettings`), perm `tenants.manage`,
  audit.

## 7 — Auditoria
- `src/lib/audit.ts` (best-effort) + `/dashboard/auditoria` (100 últimos).
- Trilha adicionada em: produtos (create/toggle), categorias (create/toggle),
  estoque (move), PDV (create/cancel), caixa (open/close c/ diff), financeiro
  (create/pay), settings (update).

## Correção de rota paralela
- Histórico do git reescrito para o proprietário (9 commits) — ver regra em
  `docs/AI_RULES.md`.

## Testes
- `npx next build --webpack` verde (27 rotas).
- Guards das novas rotas dependem da mesma base (/dashboard/* → 307 sem sessão).

## Pendente
- Imagens (Storage), planos/assinaturas, testes automatizados, logo real.
