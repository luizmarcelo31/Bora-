# 2026-09-18 — E2E 50/50 (tenant 3, prefixo `[TESTE]`, dados mantidos)

Scripts em `C:\Users\Usuario\AppData\Local\Temp\opencode\` (fora do repo — têm credencial;
regressão versionada pendente, ver ROADMAP item 9).

## E2E-1 — categorias + estoque (15/15)
Criar PRODUCT/FINANCIAL, duplicada → P2002, toggle, rename, produto vinculado,
delete com vínculo → `soft_delete` (preserva linha), delete sem vínculo → hard.

## E2E-2 — caixa + financeiro (13/13)
Abrir, fechar com sobra +2000 → RECEITA "Sobra de caixa", refechar bloqueado,
criar DESPESA/RECEITA, baixa `pay`, `paid_locked` em edit/delete, hard delete de
não-pago, `saldo = receitas − despesas`, filtro `ALL` sem where (regressão bug).

## E2E-3 — PDV espelho da action (5/5)
Venda PIX com caixa+desconto+cliente (total/caixa/estoque/financeiro conferidos),
desconto acima → `INVALID_DISCOUNT`, idempotência (mesma key → mesma venda),
cancel + estorno DESPESA + DEVOLUCAO + estoque restaurado, re-cancel bloqueado.

## E2E-4 — cross-checks (7/7)
Resumes == SUM SQL, estoque final, auditoria cobre 5 entidades, VENDA/DEVOLUCAO
referenciados, caixas, categorias ativas.

## E2E-5 — fechamento (10/10)
Update parcial, `DUPLICATE_SKU`, toggle on/off, soft-delete produto,
TRANSFERENCIA, `CLOSED_CASHBOX`, `maxDiscount` (50% bloqueia, 5% passa), login+vínculo.

## Achados que viraram backlog (depois implementados ou avaliados)
- `updateProduct` parcial zerava campos omitidos → corrigido (só toca enviados).
- Settings inexistente → defaults em código; E2E criou a linha (máx 10%).
- Prefetch `false` em tudo → avaliado e mantido (10 rotas dinâmicas/tenant).
- RECEITA original × estorno no relatório → saldo líquido correto, sem mudança.
